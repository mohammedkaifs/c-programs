#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int  string_length ( char str1[])
{
    int c=0;
    while(*(str1+c)!='\0')
    c++;
    return c;
}
int string_compare(char str1[],char str2[])
{
    int i=0;
    while(str1[i]!='\0'&& str2[i]!='\0')
    {
        int i=0;
        while(str[i]!=)
    }
}

int main()
{

    
    return 0;
}