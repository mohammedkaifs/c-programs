#include<stdio.h>
int fib_recursive(int n);
{
    return 0;
}

int fib_iterative(int n);
{
    return 0;
}

int main()
{
    int number;
    printf("\n enter the index to get fibonacci series");
    scanf("%d",&number);
    printf("\n the value of fibonacci  number at position no %d using iterative approach",fib_iterative(number));
    return 0;
}